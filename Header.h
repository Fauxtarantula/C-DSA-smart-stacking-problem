#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <list>
using namespace std;
class sauce {
public:
	void readFileFun(vector<int> &vect);
	void shelf(vector<int> &conv);
	
};
